<html>
<head>
    <title>Products</title>
</head>
<body>
    <h1>Product List</h1>
    <ul>
        {{#each productos}}
            <li>{{this.title}} - ${{this.price}}</li>
        {{/each}}
    </ul>
</body>
</html>
